# Claymore II 维护交接

## 目标与当前状态

用户把这份源码保存到 GitHub，是为了以后升级 G-Helper 时快速重用本次龙骑士二代修复。请先阅读根目录 README 和 `docs/CLAYMORE-II-UPGRADE.md`，不要重新从一代键盘映射开始。

- 上游基线：v0.279，`5b0029057541bff4cbd025e1a01e0641e11bd64e`。
- 核心修复：`32d9b3c6dcaf54f45d9c4ba0856f30b490db4c24`。
- 分支：`claymore-ii-v0.279`；现有 `main` 和其他开发分支不代表这次修复。
- 用户已报告该修正版可日常使用，并将其作为本机主力版本。之后没有由代理进行灯效测试。

## 已实现

有线识别、Gradient/Ambient 四区空间同步、二代左右主键地址映射、位置选择 UI、过期帧失效与合并。具体文件分工及限制见升级指南。

## 测试

`scripts/build-claymore.ps1 -RunChecks` 构建并运行 20 项模拟 USB 检查。测试使用生产类、独立 87 键地址 fixture、隔离的本地配置/日志，并禁用 ProgramData 配置镜像。不会调用 GHelper.Program、连接真实 HID 或抓屏。

原实现通过 10 轮离线检查；用户手动报告有线主键区修复可用。不要将此扩展为已验证无线、唤醒或长期运行。

## 用户工作约束

- 先核对真实源码、日志和配置；不猜 CLI、LED 地址或硬件接口。
- 不自动切换灯效、操作 UI 或启动新的硬件控制程序；用户希望自行观察键盘。需要安装、修改配置或管理员权限时取得相应授权。
- 构建保留 `GITHUB_ACTIONS=true`，否则上游会退出正在运行的 G-Helper。
- 官方更新会覆盖定制补丁。现有本机配置使用 `skip_updates=1`，但源码没有将它设为默认；以后维护需继续明确更新策略。
- 本仓库不保存个人配置/日志、设备路径、系统任务 XML 或本机安装脚本。不要上传这些内容。

## 下一次应做什么

用户给出新上游版本后，先比较上游是否已覆盖这些功能，再建立新升级分支并迁移核心修复及测试。保留已验证的颜色/地址/队列行为；不要覆盖原分支或其他开发工作。构建及离线检查完成后，说明剩余实机风险，等待用户同意部署。
